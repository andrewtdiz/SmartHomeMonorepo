/*************************************************************************************************

Welcome to Baml! To use this generated code, please run one of the following:

$ npm install @boundaryml/baml
$ yarn add @boundaryml/baml
$ pnpm add @boundaryml/baml

*************************************************************************************************/

// This file was generated by BAML: do not edit it. Instead, edit the BAML
// files and re-generate this code.
//
/* eslint-disable */
// tslint:disable
// @ts-nocheck
// biome-ignore format: autogenerated code
const fileMap = {
  
  "actionClassifier.baml": "enum Action {\n  LIGHTS\n  DISH\n  OVEN\n  WASHER\n  DRYER\n  THERMOSTAT\n  UNKNOWN\n}\n\nfunction ActionClassifier(query: string) -> Action {\n  client CerebrasLlama\n  prompt #\"\n    You are a home assistant. You are given a user's request and you need to classify it into an action.\n\n    {{ ctx.output_format }}\n\n    {{ _.role(\"user\") }}\n    {{ query }}\n  \"#\n}\n\n\n\n\n\n\n\n\n\n\ntest test_lights {\n  functions [ActionClassifier]\n  args {\n    query \"Turn on the lights in the living room\"\n  }\n}\n",
  "clients.baml": "// Learn more about clients at https://docs.boundaryml.com/docs/snippets/clients/overview\n\nclient<llm> CustomGPT4o {\n  provider openai\n  options {\n    model \"gpt-4o\"\n    api_key env.OPENAI_API_KEY\n  }\n}\n\nclient<llm> CerebrasQwen3 {\n  provider \"openai-generic\"\n  options {\n    base_url \"https://api.cerebras.ai/v1\"\n    api_key env.CEREBRAS_API_KEY\n    model \"qwen-3-32b\"\n  }\n}\n\nclient<llm> CerebrasLlama {\n  provider \"openai-generic\"\n  options {\n    base_url \"https://api.cerebras.ai/v1\"\n    api_key env.CEREBRAS_API_KEY\n    model \"llama-3.3-70b\"\n  }\n}\n\nclient<llm> CustomGPT4oMini {\n  provider openai\n  retry_policy Exponential\n  options {\n    model \"gpt-4o-mini\"\n    api_key env.OPENAI_API_KEY\n  }\n}\n\nclient<llm> CustomSonnet {\n  provider anthropic\n  options {\n    model \"claude-3-5-sonnet-20241022\"\n    api_key env.ANTHROPIC_API_KEY\n  }\n}\n\n\nclient<llm> CustomHaiku {\n  provider anthropic\n  retry_policy Constant\n  options {\n    model \"claude-3-haiku-20240307\"\n    api_key env.ANTHROPIC_API_KEY\n  }\n}\n\n// https://docs.boundaryml.com/docs/snippets/clients/round-robin\nclient<llm> CustomFast {\n  provider round-robin\n  options {\n    // This will alternate between the two clients\n    strategy [CustomGPT4oMini, CustomHaiku]\n  }\n}\n\n// https://docs.boundaryml.com/docs/snippets/clients/fallback\nclient<llm> OpenaiFallback {\n  provider fallback\n  options {\n    // This will try the clients in order until one succeeds\n    strategy [CustomGPT4oMini, CustomGPT4oMini]\n  }\n}\n\n// https://docs.boundaryml.com/docs/snippets/clients/retry\nretry_policy Constant {\n  max_retries 3\n  // Strategy is optional\n  strategy {\n    type constant_delay\n    delay_ms 200\n  }\n}\n\nretry_policy Exponential {\n  max_retries 2\n  // Strategy is optional\n  strategy {\n    type exponential_backoff\n    delay_ms 300\n    multiplier 1.5\n    max_delay_ms 10000\n  }\n}",
  "dishwasher.baml": "class DishwasherUpdate {\n    result \"turn_off\" | \"do_nothing\"\n}\n\nfunction HandleDishwasher(dishwasher: string, query: string) -> DishwasherUpdate {\n  client CerebrasQwen3  \n  prompt #\"\n    You are a home assistant.\n\n    You are given the state of the dishwasher and a user's request, update the dishwasher or turn it off.\n\n    You have the following dishwasher:\n    {{ dishwasher }}\n\n    {{ ctx.output_format }}\n\n    {{ _.role(\"user\") }}\n    {{ query }}\n  \"#\n}\n\ntest test_dishwasher {\n  functions [HandleDishwasher]\n  args {\n    dishwasher \"isOn: true, Finishes in: 5 minutes\"\n    query \"how much longer will it take to finish?\"\n  }\n}\n",
  "dryer.baml": "class DryerUpdate {\n    result \"turn_off\" | \"do_nothing\"\n}\n\nfunction HandleDryer(dryer: string, query: string) -> DryerUpdate {\n  client CerebrasQwen3  \n  prompt #\"\n    You are a home assistant.\n\n    You are given the state of the dryer and a user's request, update the dryer or turn it off.\n\n    You have the following dryer:\n    {{ dryer }}\n\n    {{ ctx.output_format }}\n\n    {{ _.role(\"user\") }}\n    {{ query }}\n  \"#\n}\n\ntest test_dryer {\n  functions [HandleDryer]\n  args {\n    dryer \"Finishes in: 5 minutes\"\n    query \"how much longer will it take to finish?\"\n  }\n}\n",
  "generators.baml": "// This helps use auto generate libraries you can use in the language of\n// your choice. You can have multiple generators if you use multiple languages.\n// Just ensure that the output_dir is different for each generator.\ngenerator target {\n    // Valid values: \"python/pydantic\", \"typescript\", \"ruby/sorbet\", \"rest/openapi\"\n    output_type \"typescript\"\n\n    // Where the generated code will be saved (relative to baml_src/)\n    output_dir \"../\"\n\n    // The version of the BAML package you have installed (e.g. same version as your baml-py or @boundaryml/baml).\n    // The BAML VSCode extension version should also match this version.\n    version \"0.90.2\"\n\n    // Valid values: \"sync\", \"async\"\n    // This controls what `b.FunctionName()` will be (sync or async).\n    default_client_mode async\n}\n",
  "lights.baml": "class LightsResult {\n    location string\n    name string\n    isOn bool\n}\n\nfunction HandleLights(lights: string, query: string) -> LightsResult[] {\n  client CerebrasLlama\n  prompt #\"\n    You are a home assistant.\n\n    You are given a user's request and you need to handle it.\n\n    You have the following locations and their light names:\n    {{ lights }}\n\n    {{ ctx.output_format }}\n\n    {{ _.role(\"user\") }}\n    {{ query }}\n  \"#\n}\n\n\n\ntest test_lights {\n  functions [HandleLights]\n  args {\n    lights \"Living Room: Light\\nBathroom: Light\\nLaundry: Light\\nKitchen: Light\\nBedroom: Main Light, Night Light\"\n    query \"Turn on all the lights in the house\"\n  }\n}\n",
  "oven.baml": "class OvenUpdate {\n    result \"update\" | \"turn_off\" | \"share_info\" | \"do_nothing\"\n    targetTemperature int?\n}\n\n\nfunction HandleOven(oven: string, query: string) -> OvenUpdate {\n  client CerebrasQwen3  \n  prompt #\"\n    You are a home assistant.\n\n    You are given the state of the oven and a user's message.\n    If the user just asks for current information, simply return with result: share_info.\n    Only turn the oven off if the user specifically asks you to.\n\n    You have the following oven:\n    {{ oven }}\n\n    {{ ctx.output_format }}\n\n    {{ _.role(\"user\") }}\n    {{ query }}\n  \"#\n}\n\ntest test_oven {\n  functions [HandleOven]\n  args {\n    oven \"isOn: true, isPreheating: false, currentTemperature: 350, targetTemperature: 450\"\n    query \"Did I leave the oven on?\"\n  }\n}\n",
  "thermostat.baml": "type ThermostatMode = \"heat\" | \"cool\"\n\nclass ThermostatUpdate {\n    result \"update\" | \"turn_off\" | \"share_info\" | \"do_nothing\"\n    mode ThermostatMode?\n    targetTemperature int?\n    currentTemperature int?\n}\n\nfunction HandleThermostat(thermostat: string, query: string) -> ThermostatUpdate {\n  client CerebrasQwen3  \n  prompt #\"\n    You are a home assistant.\n\n    You are given the state of the thermostat and a user's message.\n    If the user just asks for current information, simply return with result: share_info.\n    Only update the thermostat or turn it off if the user specifically asks you to.\n\n    You have the following thermostat:\n    {{ thermostat }}\n\n    {{ ctx.output_format }}\n\n    {{ _.role(\"user\") }}\n    {{ query }}\n  \"#\n}\n\ntest test_thermostat {\n  functions [HandleThermostat]\n  args {\n    thermostat \"On: true, Mode: cool, Current Temperature: 72, Target Temperature: 70\"\n    query \"Hey, what's the thermostat set to? It's like kind of cold in here.\"\n  }\n}\n",
  "washer.baml": "class WasherUpdate {\n    result \"turn_off\" | \"share_info\" | \"do_nothing\"\n}\n\nfunction HandleWasher(washer: string, query: string) -> WasherUpdate {\n  client CerebrasQwen3  \n  prompt #\"\n    You are a home assistant.\n\n    You are given the state of the washer and a user's message.\n    If the user just asks for current information, simply return with result: share_info.\n    Only turn the washer off if the user specifically asks you to.\n\n    You have the following washer:\n    {{ washer }}\n\n    {{ ctx.output_format }}\n\n    {{ _.role(\"user\") }}\n    {{ query }}\n  \"#\n}\n\ntest test_washer {\n  functions [HandleWasher]\n  args {\n    washer \"Finishes in: 5 minutes\"\n    query \"how much longer will it take to finish?\"\n  }\n}\n",
}
export const getBamlFiles = () => {
    return fileMap;
}